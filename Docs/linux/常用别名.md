- 常用别名
```
alias ll='ls -alF'
alias untar='tar -zxvf' # 解压tar文件
alias ping='ping -c 5' # ping只输出5次
alias myip='ipconfig getifaddr en0' # 局域网ip
alias wget='wget -c ' # 断点续传
alias publicip='curl ipinfo.io/ip' # 公网ip
```